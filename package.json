{
  "name": "yss-read",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "build:popup": "node esbuild.config.js",
    "build": "node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
